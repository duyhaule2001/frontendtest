import React from 'react';
import RetirementInfo from '../Common/RetirementInfo';

const RetirementInfoSap = ({ retirementInfo }) => {
    return <RetirementInfo retirementInfo={retirementInfo} />;
};

export default RetirementInfoSap;
