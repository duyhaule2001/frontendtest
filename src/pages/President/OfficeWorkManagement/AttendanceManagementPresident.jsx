import React from 'react';
import AttendanceManagement from '../../../components/Common/Attendance/AttendanceManagement';

const AttendanceManagementPresident = ({ type }) => {
    return <AttendanceManagement type={type} />;
};

export default AttendanceManagementPresident;
